export let showscreen = $state({
	titlescreen: true,
	endscreen: false
}); //Steuert die Sichtbarkeit der Startseite
//Offenbar lassen sich nur Objekte exportieren.

export const questionamount = $state({
	amount: 8
});

export let score = $state({
	score: 0
});